// config.js
const config = {
   
    // backendAPI: "http://192.168.56.102:4313",  //this is the endpoint to be accessed while using the application in production server

    backendAPI: "http://localhost:8000", //this is the endpoint to be accessed while using the application locally
    
};

export default config;
